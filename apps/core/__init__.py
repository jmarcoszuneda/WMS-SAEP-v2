"""App core — camada compartilhada de UI.

Dono do template base, partials e assets compartilhados (Tailwind, HTMX,
Alpine) e da view de home/dashboard. Não contém regra de domínio: é camada
de apresentação compartilhada, não camada de negócio.
"""
